# Tutorials

## Planned

- **Add a new orchestrator adapter** — implementing `OrchestratorAdapter` for a custom scheduler
- **Write a custom detector** — SQL-based freshness check with parameterized thresholds
- **Connect to a production Airflow cluster** — credential setup, network access, read-only role requirements
- **Deploy to EKS with ArgoCD** — end-to-end GitOps setup from scratch
